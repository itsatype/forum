<% @posts.each do |post| %>
	<div>
		<%= link_to post.title, post_path(post.id) %>
	</div>
	<div>
		<%= post.content %>
	</div>
	<div>
		<%= post.user.name %>, 	<%= post.created_at.strftime("%A, %d %b %Y") %>
	</div>
<% end%>
